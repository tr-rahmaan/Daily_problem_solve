#include<bits/stc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while(t--)
    {
        int x;
        int a,b,c;
        cin >> a >> b >> c;
        bool on=false,tw=false,te=false;
        while(3--)
        {
            if(x==1) on=true;
            if(x==2)
        }
    }

    return 0;
}
