#include <bits/stdc++.h>
using namespace std;

int main() {

    vector<int>v(3);
    for(int i = 0 ; i < n ; i++)
    {
        cin >> v[i];
    }

    for(int i = 0;i < v[i]; i++)
    {
        for
    }
    return 0;
}


